# 发布前检查清单

## 已完成

- [x] 005、006、007、008 源码已复制到公开仓库。
- [x] 依赖目录、虚拟环境、缓存、日志、数据库和大体积生成文件已排除。
- [x] 本机用户名和个人绝对路径已从公开副本移除。
- [x] 344 条评论已去除昵称、用户标识、内容标识、任务标识和平台附加身份字段。
- [x] 视频生成与数据爬取采用相互独立的大模型配置。
- [x] API Key 只通过环境变量读取，日志使用脱敏后的配置对象。
- [x] 公开版单元测试和隐私审计通过。
- [x] 005 会话管家测试通过：109 项。
- [x] 008 个人工作台测试通过：154 项。
- [x] JavaScript、Python、JSON 与 PowerShell 静态检查通过。

## 发布前仍需确认

- [x] 自主源码采用 MIT License；评论样本和第三方素材不纳入 MIT 授权范围。
- [ ] 人工抽查匿名评论正文，确认内容本身允许公开。
- [x] 006 使用锁文件完成依赖安装，111 项测试及 TypeScript 构建通过。
- [x] 006 依赖安全修复完成：更新17个包，复测111项通过，构建通过，审计为0项漏洞。
- [ ] 确认第三方素材的许可证文件与素材一起保留。
- [ ] 初始化 Git，检查首次提交清单。
- [ ] 用户确认后再连接 GitHub、推送并将仓库设为公开。
- [ ] 启用 GitHub Pages 后执行线上只读演示验收。

## 已知说明

006 已使用 `npm ci` 从锁文件完成依赖安装，随后通过 `npm audit fix` 更新17个依赖包。15个测试文件、111项测试全部通过，TypeScript构建通过，依赖审计为0项漏洞。

`New-PublicSourceSnapshot.ps1` 负责按目录和文件类型过滤源码；重新生成后仍必须运行路径可移植化处理和 `npm run audit:public`，不得跳过审计直接发布。
